# Demo images

The included PNG files are generated previews based on the offline mock campaign in `config.demo.yaml`. They are documentation examples, not real model assessment results.

Replace or supplement them with reviewed screenshots from your own authorised test environment before using the repository as a portfolio case study.
